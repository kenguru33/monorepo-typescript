{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc -b tsconfig.json",
    "clean": "rm -rf ./dist && rm -rf tsconfig.tsbuildinfo",
    "build": "npm run clean && npm run compile",
    "dev": "nodemon -e ts,js ./node_module/**/*.js --exec \"npm run compile && node ./dist/index.js\"",
    "start": "node ./dist/index.js"
  },
  "keywords": [],
  "author": "Bernt Anker",
  "license": "ISC",
  "dependencies": {
    "shared": "1.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
