{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2",
    "typescript": "^3.7.5"
  },
  "scripts": {
    "start": "lerna run start --stream --parallel",
    "build": "lerna run build"
  }
}
