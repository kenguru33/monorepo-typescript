{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2",
    "typescript": "^3.7.5"
  },
  "scripts": {
    "start:dev": "NODE_ENV=development lerna run start --stream --parallel",
    "build:dev": "NODE_ENV=development lerna run build",
    "dev": "npm run build:dev && npm run start:dev",
    "build": "NODE_ENV=production lerna run build",
    "prod": "NODE_ENV=production lerna run start --stream --parallel",
    "clean": "lerna run clean"
  }
}
